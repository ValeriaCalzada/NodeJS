const sumar = require('../sumar');
const restar = require('../restar');
const resultado = require('../resultado');


module.exports = {
    sumar: sumar,
    restar: restar,
    resultado: resultado
}